{
  "name": "rego-brick-studio",
  "version": "1.0.0",
  "private": true,
  "description": "브라우저에서 동작하는 3D 브릭 조립 스튜디오",
  "type": "module",
  "scripts": {
    "dev": "node scripts/build.mjs --serve",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "three": "0.186.0"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
